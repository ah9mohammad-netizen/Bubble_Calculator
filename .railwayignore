# Railway only needs bot/ + requirements.txt to run the service.
# Everything below is research output and is excluded from the build context.
*.md
!bot/README.md
charts/
data/
*.py
!bot/*.py
!bot/desk/*.py
.gitignore

# The metals desk ships its rules and source mapping as data, so both
# must reach the container alongside its code.
!bot/desk/config/
!bot/desk/config/*.yaml
!bot/desk/README.md
